import * as React from "react";
import Pages from "./Pages";
import PageButtonLink from "./PageButtonLink";
import MenuMeatballs from "../../icons/MenuMeatballs";

const MeatBalls = () => React.createElement(MenuMeatballs, {
  size: "small",
  color: "attention"
});

const CompactPages = ({
  pageCount,
  selectedPage,
  onPageChange,
  size
}) => {
  if (selectedPage > pageCount - 4) {
    return React.createElement(React.Fragment, null, React.createElement(PageButtonLink, {
      onPageChange: onPageChange,
      size: size
    }, 1), React.createElement(MeatBalls, null), React.createElement(Pages, {
      pageCount: 5,
      selectedPage: selectedPage,
      onPageChange: onPageChange,
      enlargement: pageCount - 4,
      size: size
    }));
  }

  if (selectedPage < 5) {
    return React.createElement(React.Fragment, null, React.createElement(Pages, {
      pageCount: 5,
      selectedPage: selectedPage,
      onPageChange: onPageChange,
      size: size
    }), React.createElement(MeatBalls, null), React.createElement(PageButtonLink, {
      onPageChange: onPageChange,
      size: size
    }, pageCount));
  }

  return React.createElement(React.Fragment, null, React.createElement(PageButtonLink, {
    onPageChange: onPageChange,
    size: size
  }, 1), React.createElement(MeatBalls, null), React.createElement(Pages, {
    pageCount: 3,
    selectedPage: selectedPage,
    onPageChange: onPageChange,
    enlargement: selectedPage - 1,
    size: size
  }), React.createElement(MeatBalls, null), React.createElement(PageButtonLink, {
    onPageChange: onPageChange,
    size: size
  }, pageCount));
};

export default CompactPages;